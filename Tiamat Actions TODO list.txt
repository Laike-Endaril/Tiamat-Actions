Make GUIConnector extending GUILine and change isWithin() to exclude areas behind nodes



Controls for adding a node (left click on empty space)

Panel for editing / reordering a node's inputs / outputs



Improve panning (drag middle click)
